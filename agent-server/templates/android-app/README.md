# MobiCoder Android App Template
# This template generates a basic Android app that can be built
# using Gradle directly on your device via the MobiCoder agent.

# Settings
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m

# To build:
# 1. ./gradlew assembleDebug    - for debug APK
# 2. ./gradlew assembleRelease  - for release APK (requires signing config)
