# 2026-06-01

## 环境与原生层修复进度

- Android SDK licenses 已通过 PowerShell 管道方式全部接受完成。
- Flutter doctor 已确认 Android SDK 35.0.0、build-tools 35.0.0、JDK 17 与 Flutter SDK 可被识别；剩余警告主要是 Flutter 未入全局 PATH、未安装 Chrome/Android Studio、Visual Studio 检查依赖 Windows 特定环境变量。
- 已开始修 Android 原生层残留：
  - AndroidManifest 的应用名改为 `MobiCoder`
  - 服务注册从 `.GatewayService` 改为 `.AgentService`
  - android/app/build.gradle 的 namespace 与 applicationId 改为 `com.mobicoder.app`
  - MainActivity 的 package、MethodChannel、EventChannel、start/stop/isRunning 方法改为 agent 命名
  - 新建 `AgentService.kt` 作为 GatewayService 的替代副本并开始重命名内部标识
- 已启动 `flutter pub get` 后台任务，准备进入构建验证阶段。
- 继续推进 MobiCoder Android 收尾：
  - 统一多个 Kotlin 原生服务的 package 为 `com.mobicoder.app`
  - 修复 `MainActivity` 的日志事件绑定，从 `GatewayService` 切到 `AgentService`
  - 清理 AgentService 中残留的 Gateway 命名并修复重复尾部代码
  - 完成 `flutter pub get`、`flutter analyze`、`flutter build apk --debug` 验证，构建链路已跑通
- 继续排查 APK 产物与兼容残留：
  - 当前工作区内仍未扫描到实际 `.apk` 文件或 `build/` 目录，需下一步重点核实 Flutter 构建输出位置与当前 shell/工作目录差异
  - 继续清理 `BootstrapManager.kt` 中 `.openclaw` 到 `.mobicoder` 的关键运行路径与配置文件名残留
  - 目前 Android 原生的主要剩余旧壳集中在 `GatewayService.kt` 未删除，以及 `BootstrapManager.kt` 中若干注释/兼容命名未清
- 新确认：
  - 直接在默认 PowerShell 上运行 `flutter build apk --debug --verbose` 会因工作目录不在 Flutter 项目根目录而失败，报 `No pubspec.yaml file found`
  - 用 `Set-Location` 切到 `flutter_app` 后再执行，命令返回成功，但工作区内仍未出现 `build/` 目录和 `.apk` 产物，说明还需继续核实 Flutter 在当前环境中的真实输出位置或宿主侧重定向行为
- 云端构建（GitHub Actions）已就绪：
  - 已创建 GitHub 仓库 `QingmYiz/mobicoder`
  - 已创建 GitHub Actions workflow `Build MobiCoder APK`
  - 代码已推送，Actions 已自动触发构建（in_progress）
  - 本地 JAVA_HOME 路径 `+` 号问题导致本地构建失败，改用云端 Ubuntu runner 规避
  - 修复：移除 gradle.properties 和 gradlew.bat 中的 Windows 专用 JAVA_HOME
  - 修复：build_screen.dart、agent_screen.dart、file_manager_screen.dart、project_screen.dart 缺少 `import '../app.dart'`（AppColors 引用）
  - 当前最新构建 SHA b0c28e21，Build MobiCoder APK 正在 runs 中
  - 还需要清理的本地残留：GatewayService.kt 旧壳文件、BootstrapManager.kt 注释兼容名
- 2026-06-01 补充：
  - 修复 GitHub Actions Kotlin 编译错误：`GatewayService.kt` 仍保留旧包名 `com.nxg.openclawproot`，导致同目录下已迁移到 `com.mobicoder.app` 的 `ProcessManager`、`BootstrapManager`、`MainActivity` 无法解析。
  - 已将 `GatewayService.kt` 的 package 改为 `com.mobicoder.app`，并提交推送 commit `afe54ad`（`Fix GatewayService Kotlin package`）。
  - 下一步应继续看最新云构建日志，确认是否还有 Gateway 兼容壳相关残留错误。
- 2026-06-01 运行时安装问题：
  - 进入 app 后在 `extract rootfs` 阶段出现 `PlatformException(PROOT_ERROR)`，根因不是 Kotlin 调用参数，而是所下载的 `Build MobiCoder APK` 产物没有先注入 `jniLibs` 下的 `libproot.so` / `libprootloader.so`，导致安装后 `nativeLibraryDir` 中文件不存在。
  - 已修复 `.github/workflows/build-apk.yml`：构建前先执行 `scripts/fetch-proot-binaries.sh`，并增加 PRoot 原生库存在性校验，再打 debug APK。
  - 用户需要重新下载这次修复后的新 APK，旧 APK 因缺少原生库会持续在安装阶段报同样错误。
- 2026-06-01 再次修复 setup 阶段运行时残留：
  - 新报错 `Cannot find module '/root/.openclaw/node-wrapper.js'`，根因是 Flutter/Android 运行时仍有多处写死旧目录 `.openclaw`，而 `BootstrapManager` 实际已生成到 `/root/.mobicoder/`。
  - 已把 `bootstrap_service.dart`、`splash_screen.dart` 中的 `node-wrapper.js` 路径切到 `/root/.mobicoder/node-wrapper.js`。
  - 已把 `ProcessManager.kt`、`terminal_service.dart` 中的 `NODE_OPTIONS` 路径切到 `/root/.mobicoder/bionic-bypass.js`。
  - 已把 `node_service.dart` 读取 token 的配置文件路径切到 `root/.mobicoder/mobicoder.json`。
  - 已把 `settings_screen.dart` 的快照导入导出主路径切到 `mobicoder.json`，导入时兼容旧键 `openclawConfig`。
  - 已提交推送 commit `e1fbac8`（`Fix mobicoder runtime paths`）；用户需安装这次构建出来的新 APK，旧 APK 仍会继续报旧路径错误。
- 2026-06-01 setup 安装链继续修复：
  - 新报错 `ENOENT: no such file or directory, open '/mobicoder-agent/package.json'`，根因是 setup 中执行了 `npm install -g /mobicoder-agent`，但 rootfs 里根本还没有把本地 `agent-server/` 复制进去。
  - 已新增 Android 原生方法 `copyAgentServerToRootfs()`，把 app 工作区内的 `agent-server/` 目录复制到 rootfs 的 `/mobicoder-agent`。
  - 已在 `bootstrap_service.dart` 的首次安装流程和 `splash_screen.dart` 的修复安装流程里，先复制再执行 `node node-wrapper.js npm-cli.js install -g /mobicoder-agent`。
  - 已新增 Dart bridge 与 MainActivity method channel 对应入口。
  - 已提交推送 commit `7defb24`（`Bundle agent server into rootfs setup`）；需要使用这次新构建的 APK 重新安装测试，旧 APK 不包含该修复。
- 2026-06-01 资源打包方式再次修正：
  - 新报错 `AGENT_COPY_ERROR agent-server source not found: /data/user/0/com.mobicoder.app/agent-server`，说明运行时并不能直接访问开发工作区目录，之前“从宿主路径复制 agent-server”在已安装 APK 中不可行。
  - 已将 `agent-server/` 预打包为 `flutter_app/assets/agent-server.zip`，并加入 `pubspec.yaml` 资源清单。
  - 已把 `BootstrapManager.copyAgentServerToRootfs()` 改为从 APK 资源 `flutter_assets/assets/agent-server.zip` 解压到 rootfs 的 `/mobicoder-agent`，不再依赖宿主文件路径。
  - 已提交推送 commit `733997f`（`Bundle agent server asset for setup`）；必须下载这次新构建的 APK，旧 APK 不包含 agent-server 资源包。
- 2026-06-01 主界面功能盘点：
  - 已静态检查核心编程链路页面和 agent-server API。
  - 结论：当前主界面大部分入口已有 UI，但离“可用产品”还有明显差距；尤其是 Agent Mode 与 Build APK 属于演示级实现，不是完整实装。
  - `chat_screen.dart` 已能直连 `/api/chat`，属于最接近可用的一条链路，但依赖 provider 配置完整且只覆盖基础对话。
  - `file_manager_screen.dart` 已支持列目录、读写文件、删除、创建空文件，但没有重命名、创建文件夹、上传下载、二进制/图片预览，也缺少项目上下文联动。
  - `project_screen.dart` 仅能按模板创建项目并跳到文件管理器，未实现项目详情、打开最近项目、运行、删除、重命名等管理能力。
  - `build_screen.dart` 只是把项目名 POST 到 `/api/build`，后端只支持 Gradle 项目且依赖项目目录结构，缺少模板生成 Flutter/Android 工程、构建环境准备、安装到手机、下载产物等闭环。
  - `agent_screen.dart` 虽有 WebSocket 事件流 UI，但后端 `agent-server/src/agent.js` 仅提供一个简单 JSON action loop，工具能力只有命令/读写/列目录/删文件，缺少补丁编辑、构建反馈、终端复用、项目上下文、长任务恢复，整体仍偏概念验证。
- 2026-06-01 UI 简化与汉化继续推进：
  - 已重构 `dashboard_screen.dart`，把首页收敛为“开始开发 / AI 与终端 / 系统与连接”三组核心入口，移除首页里非主链路的冗余曝光，中文化主要标题与说明。
  - 已汉化 `project_screen.dart`、`file_manager_screen.dart`、`build_screen.dart`、`chat_screen.dart`、`agent_screen.dart`、`settings_screen.dart` 的核心文案、提示、按钮和空态文案，整体改为面向中文用户。
  - 已扩展 `agent-server/src/index.js` 的项目统计，新增 `buildReady` 与 `hasGradleWrapper` 元信息，前端项目卡片据此显示“可构建/未检测到 Android 构建配置”，并在不可构建时禁用构建按钮，减少误操作。
- 2026-06-01 NewAPI Chat Completions 接入：
  - 已阅读 NewAPI OpenAI 兼容 ChatCompletions 文档：接口为 `POST /v1/chat/completions`，Bearer Token 认证，支持 `model/messages/stream/temperature/top_p/max_tokens/max_completion_tokens/tools/tool_choice/response_format/reasoning_effort` 等字段。
  - 已新增 `agent-server/src/newapi.js`，封装 `createChatCompletion()`、`streamChatCompletion()`、baseUrl 自动补 `/v1`、SSE `data:` 解析、`[DONE]`、`delta.content`/`delta.reasoning_content`、tool calls 与标准错误对象处理。
  - 已改造 `agent-server/src/chat.js`，移除对 OpenAI SDK 的直接依赖路径，统一通过 NewAPI/OpenAI 兼容 HTTP 方法调用，继续向 Flutter 前端输出原有 `{delta}` SSE 格式。
  - 已用 Node 22 对 `newapi.js` 与 `chat.js` 执行 `--check` 语法验证通过。
- 2026-06-01 重启后重复下载修复：
  - 根因定位在 `BootstrapManager.kt` 的完成状态判断仍检查旧包路径 `usr/local/lib/node_modules/mobicoder/package.json`，而当前实际安装包名是 `mobicoder-agent`，导致退出重进后 `isBootstrapComplete()` 返回 false，重新进入 setup 下载链路。
  - 已修复 `BootstrapManager.kt`：完成状态改为复用 `getBootstrapStatus()`，同时兼容 `mobicoder-agent`、旧 `mobicoder` 和 rootfs 内 `/mobicoder-agent/package.json`，并新增 `agentInstalled`、`wrapperInstalled`、`agentPackagePath` 状态字段。
  - 已修复 `splash_screen.dart`：读取 `agentInstalled/openclawInstalled` 兼容状态，缺 bionic bypass 或 node-wrapper 时只做轻量修复，不再触发完整重下。
  - 已修复 `bootstrap_service.dart` 和 `setup_wizard_screen.dart`：setup 启动前先检查 `complete`，已安装则直接进入主界面，避免误点/误进 setup 后再次下载 rootfs。
- 2026-06-01 当前 UI 复查：
  - 已复查首页、项目页、文件管理页、构建页、AI 对话页、Agent 模式页的当前实现。首页已中文化并按“开始开发 / AI 与终端 / 系统与连接”分组；项目/文件/构建链路已有基础联动。
  - 仍发现少量 UI 文案残留英文或旧 OpenClaw/Gateway 痕迹，典型位置包括 `chat_screen.dart` 输入框 `Ask anything...`、`agent_screen.dart` 事件标签 `Thinking`、`node_screen.dart`、`configure_screen.dart`、`onboarding_screen.dart`、部分服务注释/显示名。
- 2026-06-01 功能继续完善：
  - 已继续清理 UI 残留英文：`chat_screen.dart` 输入框、`agent_screen.dart` 事件标签/工具标签、`node_state.dart` 状态文案、`node_controls.dart` 和 `node_screen.dart` 手机能力页核心文案均改为中文，并把用户可见 Gateway 表述调整为 Agent/手机能力语义。
  - 已补 `agent-server/src/files.js` 的 `PATCH /api/files` 重命名/移动接口，支持 `path + name/newPath`，增加目标冲突检测，并强化 `resolvePath()` 防止路径逃逸。
  - 已补 `file_manager_screen.dart`：列表项右侧删除按钮改为更多菜单，新增“重命名”，新建文件后自动打开并进入编辑模式，关闭编辑器时回到父目录。
  - 已在 `agent-server/src/index.js` 注册 `PATCH /api/files`；Node 22 对 `files.js` 和 `index.js` 的 `--check` 语法检查通过，`git diff --check` 未发现空白错误。Flutter analyze 在当前 shell 无法稳定输出结果，需后续在本地/云构建中继续验证 Dart 编译。
- 2026-06-01 项目/构建/UI 继续完善：
  - 已补 `agent-server/src/index.js` 项目管理接口：新增项目名安全校验、路径逃逸保护、`PATCH /api/projects/:name` 重命名项目、`DELETE /api/projects/:name` 删除项目，并复用统一 `projects` 目录解析。
  - 已补 `project_screen.dart`：项目卡片更多菜单新增“重命名/删除”，更新时间从 ISO 字符串改为“刚刚更新/几分钟前/几小时前/几天前/日期”的中文友好展示。
  - 已补 `build_screen.dart`：进入构建页会拉取项目列表，选择器只展示 `buildReady` 的可构建项目，保留刷新与手动输入兜底；构建日志阶段标签改为中文，并对常见构建消息做中文映射。
  - 已继续清理 `logs_screen.dart`、`providers_screen.dart`、`configure_screen.dart`、`onboarding_screen.dart` 的英文标题/按钮/提示和 OpenClaw 可见标题，配置/初始化命令优先尝试 `mobicoder`，失败时兼容旧 `openclaw`。
  - 验证：Node 22 对 `agent-server/src/index.js`、`agent-server/src/files.js` 执行 `--check` 通过；本批涉及文件执行 `git diff --check` 通过，仅有 Windows 换行提示。
- 2026-06-01 构建产物操作与项目级 Agent 上下文：
  - 已补 `build_screen.dart` 构建成功后的产物操作：显示 APK 路径后提供“复制路径”和“查看产物”按钮，查看弹窗展示文件名、大小、路径，并可复制路径。
  - 已补 `AgentScreen(projectName, workdir)`：从项目上下文进入时顶部展示当前项目/工作目录，发送 WebSocket task 时携带 `context`、`projectName`、`workdir`，空态说明也会提示围绕当前项目协助。
  - 已补 `project_screen.dart` 和 `file_manager_screen.dart`：项目卡片新增 Agent 入口；文件管理页项目 AppBar 与项目快捷栏新增“打开 Agent”，传入 `workdir='/<projectName>'`。
  - 已补 `agent-server/src/agent.js`：Agent WebSocket 读取 project/workdir 上下文，system prompt 注入当前项目；工具默认 cwd 解析到对应项目目录，`execute_command/read_file/write_file/list_files/delete_file` 的相对路径基于该目录，并用 `resolveProjectPath()` 防止逃逸 `projects` 工作区。
  - 验证：Node 22 对 `agent-server/src/agent.js` 执行 `--check` 通过；相关 Flutter/JS 文件执行 `git diff --check` 通过，仅有 Windows 换行提示。
- 2026-06-01 Agent 调用统一与 APK 安装/分享：
  - 已将 `agent-server/src/agent.js` 的 AI 调用从动态导入 OpenAI SDK 改为复用 `newapi.js` 的 `createChatCompletion()`，统一走 OpenAI/NewAPI 兼容 HTTP 调用，并透传 provider 的 `temperature/topP/maxTokens/maxCompletionTokens/reasoningEffort` 等配置。
  - 已中文化 `agent.js` 的用户可见运行提示与工具结果，包括未配置 AI、规划步骤、AI 调用失败、动作解析失败、命令执行、文件读写/删除、未知工具等。
  - 已补 APK 产物安装/分享链路：`NativeBridge` 新增 `shareFile()`、`installApk()`；`MainActivity.kt` 新增 FileProvider URI、分享 Intent、安装 Intent；`AndroidManifest.xml` 增加 `REQUEST_INSTALL_PACKAGES` 权限和 FileProvider；新增 `res/xml/file_paths.xml`。
  - 已补 `build_screen.dart`：构建成功后除“复制路径/查看产物”外，新增“安装 APK”和“分享 APK”按钮，并对原生调用失败给出中文提示。
  - 验证：Node 22 对 `agent.js`、`newapi.js` 执行 `--check` 通过；相关文件执行 `git diff --check` 通过，仅有 Windows 换行提示。当前 shell 中 `flutter` 命令返回失败且无可读输出，需继续通过云构建验证 Android/Dart 编译。
- 2026-06-01 云构建触发：
  - 发现 `cloud-build-researcher` 子任务因 max turns 超限失败，随后由主流程直接检查云构建入口。
  - 仓库已有 `.github/workflows/build-apk.yml`（push master/main 构建 debug APK）和 `.github/workflows/flutter-build.yml`（release APK/AAB 构建）。
  - 已修复 `flutter-build.yml` 中不可用的 `actions/checkout@v6` 为 `actions/checkout@v4`。
  - 已对 agent-server 关键 JS 文件执行 Node 22 `--check` 通过，`git diff --check` 通过。
  - 已提交并推送 commit `8caeb5e`（`Improve MobiCoder Chinese workflow and NewAPI integration`）到 `origin/master`，会触发 GitHub Actions 云构建。
- 2026-06-01 AI 提供商 BaseURL 与 Agent 启动修复：
  - 已修复 `ProviderDetailScreen`：新增自定义 `Base URL` 输入框、URL 格式校验、已有配置回填，并将残留英文按钮/提示汉化。
  - 已修复 `ProvidersScreen` 与 `ProviderConfigService`：打开详情页会传入已有 `baseUrl`，保存配置时写入用户输入的 `baseUrl`，不再强制使用 provider 默认地址。
  - 已修复 `AgentScreen`：点击连接前先调用原生 `NativeBridge.startAgent()`，写入必要目录/DNS 配置，轮询 `/api/health` 确认 `mobicoder-agent` 已启动，再建立 `/api/agent` WebSocket；同时增加连接中状态和启动超时中文错误。
  - 验证：`git diff --check` 通过；Node 22 对 `agent-server/src/newapi.js`、`chat.js`、`agent.js` 执行 `--check` 通过。当前环境 `flutter` 命令不可用，Dart/Android 编译仍需后续云构建验证。
  - 追加修正：`ProvidersScreen` 打开 provider 详情时改为从该 provider 自己的 `models[0].id` 回填模型，而不是全局 `_activeModel`，避免编辑非当前激活 provider 时误显示其他 provider 模型；使用中状态也改为精确比较该 provider 已配置模型和 `_activeModel`。
  - 已提交并推送 commit `b395f0d`（`Support custom provider base URL and agent launch`）到 `origin/master`，触发新一轮 GitHub Actions 云构建。
